# 安全评估协议设计文档

## 一、原文档问题分析

### 原协议定义（专用安全评估接口技术文档3.1.pdf）

#### 固定头部问题
- **声明长度**：32字节
- **实际定义**：4bit + 2bit + 2bit = 8bit = 1字节
- **缺失**：31字节未说明用途

#### 字段设计缺陷
| 字段 | 定义 | 问题 |
|------|------|------|
| 消息类型 | 2bit | 只能表示0-3共4种类型，扩展性差 |
| 消息大小 | 2bit | 只能表示4种长度编码方式，不灵活 |

#### 扩展载荷问题
- 长度：288字节，过于冗长
- 实际有效数据：IP + MAC + trust，远不需要288字节

---

## 二、改进版协议定义

### 2.1 固定头部（16字节）

| 偏移 | 字段 | 类型 | 长度 | 描述 |
|------|------|------|------|------|
| 0 | 魔数 | u32 | 4字节 | 协议标识 `0x5A454B50` ("ZEKP") |
| 4 | 版本 | u8 | 1字节 | 协议版本，当前为 `0x01` |
| 5 | 消息类型 | u8 | 1字节 | 消息类型定义见下表 |
| 6 | 载荷长度 | u16 | 2字节 | 扩展载荷长度（大端序） |
| 8 | 序列号 | u32 | 4字节 | 请求响应匹配、防重放 |
| 12 | 时间戳 | u32 | 4字节 | Unix时间戳（秒），时效性校验 |

### 2.2 消息类型定义

| 类型值 | 名称 | 方向 | 说明 |
|--------|------|------|------|
| 0x01 | EvalRequest | 客户端→设备 | 安全评估请求 |
| 0x02 | EvalResponse | 设备→客户端 | 安全评估响应 |
| 0x03 | TrustGrant | 设备→客户端 | 信任IP下发 |
| 0x04 | IsolateNotify | 设备→客户端 | 隔离通知 |

### 2.3 扩展安全评估载荷（16字节）

| 字段 | 类型 | 长度 | 描述 | 示例 |
|------|------|------|------|------|
| ip | u32 | 4字节 | 设备IP（网络字节序） | 0xC0A8125A (192.168.18.90) |
| mac | u8[6] | 6字节 | 设备MAC地址 | E0:BE:03:42:A2:72 |
| trust | u8 | 1字节 | 安全评估值 (0-100) | 90 |
| reserved | u8[5] | 5字节 | 保留，填充0 | - |

### 2.4 完整消息结构

```
+----------------+----------------+
|  固定头部      |  扩展载荷      |
|  16 字节       |  16 字节       |
+----------------+----------------+
       ↓                ↓
    明文传输         加密传输
```

**总消息长度：32字节**

---

## 三、Rust 结构体定义

```rust
/// 固定头部 (16字节)
#[repr(C, packed)]
pub struct PacketHeader {
    pub magic: u32,        // 魔数 0x5A454B50
    pub version: u8,       // 版本号
    pub msg_type: u8,      // 消息类型
    pub payload_len: u16,  // 载荷长度
    pub seq_num: u32,      // 序列号
    pub timestamp: u32,    // 时间戳
}

/// 安全评估载荷 (16字节)
#[repr(C, packed)]
pub struct EvalPayload {
    pub ip: u32,           // 设备IP
    pub mac: [u8; 6],      // 设备MAC
    pub trust: u8,         // 安全评估值
    pub reserved: [u8; 5], // 保留字段
}

/// 完整消息
pub struct SecurityEvalPacket {
    pub header: PacketHeader,
    pub payload: EvalPayload,
}
```

---

## 四、方案对比

| 项目 | 原方案 | 改进方案 |
|------|--------|----------|
| 固定头部 | 32字节（定义不完整） | 16字节（完整定义） |
| 扩展载荷 | 288字节 | 16字节 |
| 总长度 | 320字节 | 32字节 |
| 消息类型 | 2bit（4种） | 8bit（256种） |
| 防重放 | 无 | 序列号+时间戳 |
| 协议识别 | 无 | 魔数校验 |
| 版本管理 | 无 | 版本号字段 |

---

## 五、通信流程

### 5.1 正常流程

```
端盾客户端                    专联设备
    |                            |
    |--- EvalRequest ----------->|  (1) 安全评估请求
    |                            |
    |<-- EvalResponse -----------|  (2) 安全评估响应
    |                            |
    |<-- TrustGrant -------------|  (3) 信任IP下发
    |                            |
    |   (每30分钟重复步骤1-3)     |
    |                            |
```

### 5.2 异常流程

```
端盾客户端                    专联设备
    |                            |
    |--- EvalRequest ----------->|  评估值 < 80
    |                            |
    |<-- IsolateNotify ----------|  隔离通知
    |                            |
    |   (客户端被隔离)            |
```

### 5.3 超时处理

- 客户端：每30分钟发送一次评估请求
- 服务端：30分钟内未收到请求，删除信任IP，隔离客户端

---

## 六、UDP 通信参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 目的端口 | 62201 | 可通过端盾下发配置 |
| 目的IP | 网关IP | 安全评估IP |
| 协议 | UDP | 无连接传输 |

---

## 七、安全考虑

1. **载荷加密**：扩展载荷部分需加密传输
2. **防重放**：序列号递增，时间戳校验时效性
3. **协议识别**：魔数防止误解析
4. **版本兼容**：版本号字段支持协议升级
