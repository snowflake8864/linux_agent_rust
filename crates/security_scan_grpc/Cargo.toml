[package]
name = "security_scan_grpc"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.14"
tokio = { version = "1", features = ["full"] }
tokio-stream = "0.1"
futures = "0.3"
grpc_gateway = { path = "../grpc_gateway" }
logging = { path = "../logging" }
log = "0.4"
